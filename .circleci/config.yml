version: 2
jobs:
        build:
                docker:
                        - image: debian:stretch
                context:
                        - 'Test Context'
                steps:
                        - run:
                                name: Sleep 1s
                                command: sleep 1s
                        - run:
                                name: Greeting
                                command: echo "dHello, world:_${testvar}!"

        print_date:
                docker:
                        - image: debian:stretch
                steps:
                        - run:
                                name: Print current time
                                command: date

workflows:
        version: 2
        build_stuff:
                jobs:
                        - build
                        - print_date
