version: 2.1

jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - run:
          name: Sleep 15s
          command: sleep 15s
      - run:
          name: Greeting
          command: echo "Hello, 🌎!"

  print_date:
    docker:
      - image: debian:stretch
    steps:
      - run:
          name: Print current time
          command: date

workflows:
  version: 2
  build_stuff:
    jobs:
      - hold_it_partner:
          type: approval
          context: test-context-cli
      - build:
          requires:
            - hold_it_partner
      - print_date
